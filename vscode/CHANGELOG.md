# Changelog

## 0.0.2

- Rename package to `aweshelf-ext` (display name remains "Aweshelf")
- Fix activity bar icon: use filled SVG for proper mask rendering
- Add `vsce publish` step to CI workflow
- Remove trailing commas in package.json for strict JSON compatibility

## 0.0.1

- Initial release
- Bookmark sidebar grouped by category
- Search, sort, filter bookmarks
- Resume bookmarks with profile support
- Edit and remove bookmarks
- Copy session ID and resume command
